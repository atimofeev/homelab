---
apiVersion: v1
kind: ConfigMap

metadata:
  name: atuin-config
  namespace: atuin

data:
  server.toml: |-
    host = "0.0.0.0"
    port = 8888
    open_registration = true

    [metrics]
    enable = true
    host = "0.0.0.0"
    port = 9001

