---
apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: certs-and-issuers
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-8"

spec:
  project: default
  source:
    repoURL: https://github.com/atimofeev/homelab
    targetRevision: HEAD
    path: apps/certs-and-issuers
    directory:
      exclude: main.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: cert-manager
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
