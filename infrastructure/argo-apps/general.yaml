---
applications:

  bento-pdf:
    project: default
    source:
      repoURL: https://github.com/atimofeev/homelab
      targetRevision: HEAD
      path: applications/bento-pdf
    destination:
      server: https://kubernetes.default.svc
      namespace: bento-pdf
    syncPolicy:
      automated:
        prune: true
        selfHeal: true
      syncOptions:
        - CreateNamespace=true

  vert:
    project: default
    source:
      repoURL: https://github.com/atimofeev/homelab
      targetRevision: HEAD
      path: applications/vert
    destination:
      server: https://kubernetes.default.svc
      namespace: vert
    syncPolicy:
      automated:
        prune: true
        selfHeal: true
      syncOptions:
        - CreateNamespace=true
